/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['hanoicomputercdn.com', 'cdn.tgdd.vn'], // Add the additional domain here
    },
  };
  
  module.exports = nextConfig;
  